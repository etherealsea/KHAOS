# 2026-04-09 A股 Horizon 训练恢复记录

## 本次操作

- 恢复时间：`2026-04-09 13:32:09`（Asia/Shanghai）
- 恢复对象：`horizon_single_precision_v1`
- 恢复模式：`formal + resume`
- 恢复命令：

```powershell
python "Finance/03_实验与验证/脚本/测试与临时脚本/run_teacher_first_ashare_ablation_train.py" --formal-only --experiments horizon_single_precision_v1 --resume
```

## 恢复前确认

- 没有残留 `python` 训练进程。
- 安全断点仍然存在：
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/horizon_single_precision_v1_resume.pth`
- 最近已落盘 epoch 仍为 `epoch 1`。
- 当前恢复继续沿用：
  - `split_scheme = rolling_recent_v1`
  - `horizon_family_mode = single_cycle`
  - `horizon_search_spec.min_horizon = 20`
  - `warm_start_weights_only / resume` 分离后的新逻辑

## 恢复后即时观测

- 新训练进程已启动，PID：`9472`
- 设备：`cuda`
- 训练日志已继续写入，确认进入：`EPOCH 2/18`
- 日志内确认：
  - `Config fingerprint: 03c0df110f0a463a`
  - `Global horizon grid size: 101`
  - `[RESUME] Loaded checkpoint: epoch=1, best_score=-inf, best_val_loss=inf`
- 启动后约 45 秒观测到：
  - 进程 CPU 已累计到 `66.640625`
  - 常驻内存约 `1.78 GB`
  - 主日志最后写入时间更新到 `2026-04-09 13:33:35`

## 当前状态判断

- 训练已经真实恢复，不是空转或瞬时退出。
- 目前仍处于 `epoch 2` 进行中，因此 `epoch_metrics.jsonl` 与 `resume.pth` 暂时还未写出新的安全落盘结果。
- 下一次判断是否有效改进，必须等 `epoch 2` 完整落盘后再看：
  - `breakout_precision`
  - `reversion_precision`
  - `public_below_directional_violation_rate`
  - `signal_frequency`
  - `selected_horizon_*`
  - `score_veto`

## 备注

- 现阶段不能宣称已经达到“可用级高精度”，因为最终 holdout 指标尚未产生。
- 若 `single_cycle` 在后续 epoch 仍持续被 precision floor / public violation / oversignal 否决，则下一正式候选实验为：
  - `horizon_adaptive_precision_v1`
