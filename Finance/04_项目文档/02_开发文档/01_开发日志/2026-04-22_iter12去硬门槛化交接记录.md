# 2026-04-22 iter12 去硬门槛化交接记录

## 1. 交接定位

- 当前分支：`codex/iter12-origin-main-sync`
- 当前提交目标：保存一份可推送到 GitHub 的 iter12 中间检查点，供云端 AI 继续接力
- 本次检查点性质：`WIP / handoff checkpoint`
- 明确不纳入本次提交的内容：
  - `runs/` 目录下训练产物
  - 学术报告 `.docx`
  - 其它与 iter12 去硬门槛化无关的实验文件

## 2. 本次已经落地的代码改动

### 2.1 horizon 搜索规格补齐

文件：
- `Finance/02_核心代码/源代码/khaos/数据处理/ashare_dataset.py`

已完成：
- `normalize_horizon_search_spec()` 新增显式 horizon 列表支持
- `build_horizon_candidates()` / `build_global_horizon_grid()` 支持 `6,10,14,20` 这类列表输入
- 兼容 JSON 风格数组与逗号分隔字符串

目的：
- 为 iter12 runner 默认传入 `horizon_search_spec=6,10,14,20` 做铺垫
- 避免训练主链路退化回旧的单 horizon 行为

### 2.2 结构侧软门控原型

文件：
- `Finance/02_核心代码/源代码/khaos/模型定义/kan.py`

已完成：
- 为 `KHAOS_KAN` 增加：
  - `gate_mode`
  - `gate_floor_breakout`
  - `gate_floor_reversion`
  - `gate_anneal_fraction`
- 新增软门控相关辅助逻辑与 epoch 进度函数
- 将 iterA4/iterA5 多尺度路径中的 detached hard gates 改为可参与反向传播的 soft gate 路径
- 新增门控诊断输出：
  - `compression_gate`
  - `directional_gate`
  - `gate_floor_hit`

当前状态说明：
- 文件已能通过编译
- 但这部分是本轮改动最核心也最敏感的区域，云端 AI 接手后仍应优先复核数值行为和梯度流

### 2.3 loss 侧 guarded_v3 雏形

文件：
- `Finance/02_核心代码/源代码/khaos/模型训练/loss.py`

已完成：
- 新增 `shortT_discovery_guarded_v3`
- `set_epoch()` 中加入课程式阶段控制
- horizon signal calibration 从单边压制改为区间带约束
- 新增并接入以下结构化 loss 项：
  - event gap
  - hard negative
  - direction consistency
  - continuation suppression
  - signal calibration

当前状态说明：
- 已经从“压制型 guarded_v2”向“探索优先 guarded_v3”迈出第一步
- 但具体权重和课程曲线是否完全达到计划，还需要继续校正

### 2.4 train 侧软评分 / 诊断 / 续训状态增强

文件：
- `Finance/02_核心代码/源代码/khaos/模型训练/train.py`

已完成：
- 新增评分配置：`iter12_soft_guarded_precision_first`
- 新增辅助函数：
  - `is_soft_iter12_profile`
  - `compute_signal_health`
  - `compute_threshold_selection_utility`
  - `compute_catastrophic_guard`
  - `resolve_dataset_cache_fingerprint`
- threshold 选择逻辑已支持 soft utility 评分
- 汇总指标新增：
  - `compression_gate_mean/std`
  - `directional_gate_mean/std`
  - `gate_floor_hit_rate`
  - `pred_breakout_std`
  - `pred_reversion_std`
  - `directional_support_rate`
- resume 状态增强，已纳入：
  - `best_checkpoint`
  - `best_score_components`
  - `fitted_thresholds`
  - `completed_epoch`
  - `catastrophic_state`
  - `dataset_cache_fingerprint`
- `try_resume_training()` 已增加 `dataset_cache_fingerprint` 校验

当前状态说明：
- 续训能力已经明显比旧版完整
- 但距离“严格的完整状态续训”仍建议继续补：
  - `global_horizon_grid`
  - `config_fingerprint`
  - 更明确的恢复失败提示与拒绝逻辑

### 2.5 iter12 runner 与测试骨架

文件：
- `scripts/run_iter12_multiasset_closed_loop.py`
- `scripts/run_iter12_multiasset_formal_live.ps1`
- `Finance/03_实验与验证/脚本/test_iter12_guarded_precision_first.py`
- `Finance/03_实验与验证/脚本/test_iter12_multiasset_runner.py`
- `Finance/03_实验与验证/脚本/test_iter12_recent_split_coverage.py`

已完成：
- 新增 iter12 专用多资产 runner
- runner 支持：
  - `training_subdir=None` 时直接读取 `<data_dir>/training_ready`
  - smoke / formal 预设
  - dataset cache 目录
  - resume_mode
  - recent split 参数
  - iter12 相关 loss / score wiring
- 补充了 iter12 的基础测试文件

### 2.6 iter10 公共脚本顺手做了可复用修正

文件：
- `scripts/run_iter10_multiasset_closed_loop.py`
- `scripts/generate_iter10_multiasset_report.py`

已完成：
- 去掉固定写死的 `/workspace/...` 路径，改为从项目根自动定位
- `run_iter10_multiasset_closed_loop.py` 支持 `training_subdir` 为空时直接使用 `training_ready`
- `generate_iter10_multiasset_report.py` 增加自定义标题参数，并整理了报告渲染逻辑

说明：
- 这两处虽然名义上是 iter10 文件，但实际上已成为 iter12 runner 的可复用基础设施，因此一并纳入本次交接提交

## 3. 已完成验证

### 3.1 编译检查

以下文件已通过静态编译检查：

- `Finance/02_核心代码/源代码/khaos/数据处理/ashare_dataset.py`
- `Finance/02_核心代码/源代码/khaos/模型定义/kan.py`
- `Finance/02_核心代码/源代码/khaos/模型训练/loss.py`
- `Finance/02_核心代码/源代码/khaos/模型训练/train.py`
- `scripts/run_iter12_multiasset_closed_loop.py`
- iter12 相关测试文件

### 3.2 测试通过情况

以下测试已确认通过：

1. `Finance/03_实验与验证/脚本/test_iter12_guarded_precision_first.py`
2. `Finance/03_实验与验证/脚本/test_iter12_multiasset_runner.py`
3. `Finance/03_实验与验证/脚本/test_iter12_recent_split_coverage.py`

## 4. 目前还没有做完的部分

以下内容仍属于“进行中”，不要把当前检查点误判为最终可 formal 的 iter12：

- `kan.py` 的软门控仍需结合真实训练日志确认：
  - slope 退火是否按计划推进
  - gate floor 命中率是否过高
  - 是否仍存在输出塌缩
- `train.py` 中灾难保护逻辑、recent score 逻辑与旧 veto 逻辑之间仍需要再做一轮梳理，避免旧逻辑残留主导 best checkpoint
- `shortT_discovery_guarded_v3` 还需要与最终计划逐项核对权重、课程因子和默认 profile wiring
- iter12 runner 目前虽然已经具备 smoke/formal 外壳，但还应继续对齐：
  - `gate_mode=soft_annealed`
  - `horizon_search_spec`
  - 完整 resume fingerprint
  - 文档最终指定的 CLI 默认值
- 还没有重新启动一轮新的“正式去硬门槛化 smoke/formal”训练，本次提交只是保存代码与交接状态

## 5. 云端 AI 接力建议顺序

建议严格按下面顺序继续：

1. 先复核 `kan.py` / `loss.py` / `train.py` 当前 wiring 是否完整对齐“去硬门槛化重构计划”
2. 补完 runner 到训练主程序的参数贯通，特别是：
   - `horizon_search_spec`
   - `gate_mode`
   - gate floor / anneal 参数
   - catastrophic guard 参数
3. 增补以下测试：
   - 软门控梯度测试
   - threshold utility / 阈值上界测试
   - catastrophic-only hard stop 测试
   - resume fingerprint 恢复测试
4. 先跑一轮从零开始的 smoke，再决定 formal
5. smoke 验证重点不要再看旧 `60m composite` 硬门槛，而是看：
   - `public_below_directional_violation_rate`
   - `recent_min_precision`
   - `60m precision`
   - `60m hard_negative_rate`
   - `pred_breakout_std / pred_reversion_std`
   - 门控均值与 floor hit rate

## 6. 一句话结论

当前仓库已经保存到一个“可以继续接力开发 iter12 去硬门槛化主线”的中间节点，但还不是最终可交付训练结论；这次提交的价值是把代码骨架、测试骨架、日志说明和后续施工路径固定下来，便于云端 AI 无缝继续。
