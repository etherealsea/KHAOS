# 2026-04-08 A股 Horizon 训练暂停与重启说明

## 当前状态

- 用户已明确要求暂停训练。
- `horizon_single_precision_v1` 正式训练已停止，不再有存活的 `python` 训练进程。
- 本次停止发生在训练已经进入 `epoch 2/18` 期间，因此 `epoch 2` 的未落盘中间进度已经放弃。
- 当前最晚安全断点仍然是 `epoch 1` 结束后写出的断点文件。

## 已确认的安全断点

- 实验名：`horizon_single_precision_v1`
- 训练模式：`formal`
- 方案族：`single_cycle`
- 数据切分：`rolling_recent_v1`
- 断点文件：
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/horizon_single_precision_v1_resume.pth`
- 指标文件：
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/epoch_metrics.jsonl`
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/per_fold_metrics.jsonl`
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/per_timeframe_metrics.jsonl`
  - `Finance/02_核心代码/模型权重备份/teacher_first_ashare/horizon_single_precision_v1/horizon_discovery_summary.json`
- 训练日志：
  - `logs/teacher_first_ashare/horizon_single_precision_v1/horizon_single_precision_v1.log`

## 当前最晚已落盘结果

- 最新安全 epoch：`1`
- `composite_score = 0.2344823025750849`
- `breakout_precision = 0.17909392372964011`
- `reversion_precision = 0.40496149722195146`
- `breakout_f1 = 0.2501042810566012`
- `reversion_f1 = 0.47671366485463074`
- `direction_macro_f1 = 0.9666111657039559`
- `public_below_directional_violation_rate = 0.9924146583499257`
- 平均信号频率：
  - breakout: `0.4524638272755857`
  - reversion: `0.45272860665368514`
- horizon 选择均值：
  - breakout: `118.6231460571289`
  - reversion: `20.018766403198242`
- veto 原因：
  - `fold_3_precision_floor(0.164,0.416)`
  - `fold_3_public_violation(0.992)`
  - `fold_4_precision_floor(0.156,0.423)`
  - `fold_4_public_violation(1.000)`
  - `signal_frequency(0.456>0.318)`

## 解释

- 新的 A 股 horizon 自发现、rolling recent、weights-only warm start、recent-aware score、veto 机制都已经真实跑起来了。
- 但当前 `single_cycle` 正式训练在第一个安全 epoch 上表现仍然明显不达标，尤其是：
  - breakout precision 很低
  - public directional violation 极高
  - 信号频率过密
- 因为本次是手动暂停，当前没有生成新的 `best checkpoint`；恢复时应从 `resume.pth` 继续，而不是重新 warm start。

## 用户下次要求“重启训练”时的默认动作

若用户没有改方案，默认直接从当前断点恢复：

```powershell
python "Finance/03_实验与验证/脚本/测试与临时脚本/run_teacher_first_ashare_ablation_train.py" --formal-only --experiments horizon_single_precision_v1 --resume
```

若用户要求切换到自适应多周期方案，则启动：

```powershell
python "Finance/03_实验与验证/脚本/测试与临时脚本/run_teacher_first_ashare_ablation_train.py" --formal-only --experiments horizon_adaptive_precision_v1
```

## 暂停约定

- 在用户明确要求重启前，不主动恢复训练。
- 下次恢复前，先再次检查：
  - 是否已有残留训练进程
  - `resume.pth` 是否仍存在
  - `config_fingerprint` 是否与当前实验配置一致
